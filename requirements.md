## Packages
framer-motion | For smooth interface animations and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely
react-hook-form | For efficient, flexible forms
zod | Schema validation (already in base, but emphasizing usage)
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
- Tailwind Config needs to extend font families for 'Oxanium' (display) and 'Space Grotesk' (body/numbers)
- The app uses a "Dark Gaming" aesthetic with Neon Green accents
- Logic follows the specific Bear Trap Ratio Calculator rules provided
- Calculations are performed client-side for instant feedback, then saved to server via API
